"""Reusable inversion core: the encoder + vec2text inverter, recovery metric, and
defenses. Shared by the `inversion` scan module (the tool) and the defense study, so
the two never diverge (see docs/ARCHITECTURE.md).
"""
